import {View, Text} from 'react-native';
import React from 'react';
import styles from './styles/Other';

const ZoneScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={{color: '#000'}}>Zone Screen</Text>
    </View>
  );
};

export default ZoneScreen;
