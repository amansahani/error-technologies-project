import {Text, View} from 'react-native';
import React, {Component} from 'react';
import styles from './styles/Other';

const HistoryScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={{color: '#000'}}>History Screen</Text>
    </View>
  );
};

export default HistoryScreen;
