import { StyleSheet } from "react-native";

const styles = () => StyleSheet.create({

    textInput: {
      height: 40,
      borderColor: '#ccc',
      borderWidth: 1,
      marginBottom: 10,
      padding: 10,
      borderRadius: 10,
      width : '100%',
    },
    
  });
export default styles;