import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    Container:{
        width: '30%',
        height: '4%',
        borderRadius: 20,
        borderWidth: 0.5,
        borderColor: '#EB3924',
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-around'
      },
      testText: {color: "#EB3924", fontSize: 12}

})
export default styles;